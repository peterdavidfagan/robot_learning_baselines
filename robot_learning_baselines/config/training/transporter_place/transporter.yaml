num_epochs: 1000
batch_size: 4

# checkpoint manager
checkpoint_dir: ${config.project.base_path}/.checkpoints/${config.wandb.experiment_name}/transporter_place
max_checkpoints: 2
save_interval: 5

optimizer:
  _target_: optax.adam
  learning_rate: 1e-4
